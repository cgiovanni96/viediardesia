import icons from "./icons";
import paths from "./paths";
import { pointsByCategories } from "./points";
import zones from "./zones";

const data = {
  icons: icons,
  paths: paths,
  points: pointsByCategories,
  zones: zones,
};

export default data;
